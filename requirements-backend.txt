# CP0.3B-0: Windows x64 / Python 3.12.x. Run from the repository root.
# Authoritative backend requirements; do not merge prototype/requirements.txt.
# Install the exact CUDA wheels first as documented in docs/backend_runtime.md.
-c constraints-backend.txt

# One editable installation of the existing source checkout. Its pyproject.toml
# owns the upstream dependency list. Pins below record the audited baseline.
-e ./external/OmniVoice
transformers==5.17.0
accelerate==1.15.0
numpy==2.5.3
soundfile==0.14.0
librosa==1.0.0
pydub==0.25.1
gradio==6.27.0
tensorboardX==2.6.5
webdataset==1.0.2

# Application/core, forthcoming API, and deterministic regression tests.
psutil==7.2.2
fastapi==0.141.1
uvicorn==0.52.4
pydantic==2.13.5
pytest==9.1.1

# Secondary TTS provider (prototype/providers/piper.py): fixed per-language
# voices for the six languages whose candidate voice package is PENDING (not
# REJECTED) in docs/model_license_matrix.md. piper-tts is GPL-3.0-or-later
# and NOT yet cleared for commercial distribution - see that doc before
# shipping this to a paying customer. Not installed automatically; run
# `pip install -r requirements-backend.txt` again after pulling this change.
piper-tts==1.8.0
onnxruntime==1.30.0

# Optional BYOK machine-translation step (backend/services/translation_service.py).
# Only imported once a customer saves their own Gemini API key in Settings -
# this app never ships or embeds a shared key. See
# claude/nghien-cuu-dich-may-mt-16-09.md for the tradeoffs of this path.
# Not exact-pinned like the packages above (unlike them, this one was not
# fully audited against constraints-backend.txt) - verify against the
# then-current release at https://pypi.org/project/google-genai/ before
# installing, and pin an exact version here once confirmed.
google-genai>=2.0.0
