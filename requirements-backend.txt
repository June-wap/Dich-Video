# CP0.3B-0: Windows x64 / Python 3.12.x. Run from the repository root.
# Authoritative backend requirements; do not merge prototype/requirements.txt.
# Install the exact CUDA wheels first as documented in docs/backend_runtime.md.
-c constraints-backend.txt

# Application/core, forthcoming API, and deterministic regression tests.
psutil==7.2.2
fastapi==0.141.1
uvicorn==0.52.4
pydantic==2.13.5
pytest==9.1.1
# Pinned SentencePiece for VieNeu/NeuCodec/torchtune compatibility.
# 0.2.2 introduced pybind11/mod_gil_not_used with MSVC 14.51 causing 0xC0000005 crashes on clean VMs.
sentencepiece==0.2.1

# CP2: official VieNeu 0.5B integration.  The source revision is immutable;
# the adapter explicitly selects pnnbao-ump/VieNeu-TTS rather than upstream's
# 0.3B default.  This dependency is installed at build time, never cloned by
# the customer runtime.
vieneu @ git+https://github.com/pnnbao97/VieNeu-TTS.git@b116f820dc9ed8fae70a5570389928ecad233175

# Secondary TTS provider (prototype/providers/piper.py): fixed per-language
# voices for the six languages whose candidate voice package is PENDING (not
# REJECTED) in docs/model_license_matrix.md. piper-tts is GPL-3.0-or-later
# and NOT yet cleared for commercial distribution - see that doc before
# shipping this to a paying customer. Not installed automatically; run
# `pip install -r requirements-backend.txt` again after pulling this change.
piper-tts==1.8.0
onnxruntime==1.30.0

# Optional BYOK machine-translation step (backend/services/translation_service.py).
# Only imported once a customer saves their own Gemini API key in Settings -
# this app never ships or embeds a shared key. See
# claude/nghien-cuu-dich-may-mt-16-09.md for the tradeoffs of this path.
# Not exact-pinned like the packages above (unlike them, this one was not
# fully audited against constraints-backend.txt) - verify against the
# then-current release at https://pypi.org/project/google-genai/ before
# installing, and pin an exact version here once confirmed.
google-genai>=2.0.0

# Cửa sổ desktop thật thay vì tab trình duyệt (scripts/run_app.py, 17/09).
# `pythonnet` cho phép pywebview dùng renderer WebView2 (Chromium, giống
# Edge hiện đại) trên Windows thay vì rơi về MSHTML (IE cũ, không render
# đúng React) - máy đích cần có WebView2 Runtime (thường đã có sẵn trên
# Windows 10/11 cập nhật, cùng gói với Edge; nếu thiếu, tải tại
# https://developer.microsoft.com/microsoft-edge/webview2/). Không bắt
# buộc phải cài 2 gói này - scripts/run_backend.ps1 tự kiểm tra và rơi về
# mở tab trình duyệt như trước nếu thiếu. Chưa ghim phiên bản chính xác
# (chưa audit như các gói ở trên) - kiểm tra bản mới nhất trước khi cài.
pywebview>=5,<6
pythonnet>=3.0.0

# Windows DPAPI (backend/services/translation_service.py): encrypts the
# customer's Gemini API key at rest, scoped to the current Windows user
# account, instead of storing it as plain text in metadata.sqlite3 - see
# claude/checklist-bao-mat-truoc-dong-goi-17-09.md muc 6. Imported lazily
# and optionally - if this is not installed, the app falls back to the
# previous plaintext storage rather than failing, but a real release build
# must have this installed in the staged runtime before shipping. Not
# exact-pinned (not audited against constraints-backend.txt like the top
# block) - verify the latest release at
# https://pypi.org/project/pywin32/ before installing.
pywin32>=306
